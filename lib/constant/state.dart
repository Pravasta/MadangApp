enum GlobalStateStatus { initial, loading, succes, error }

enum HomeStatusState { initial, loading, hasData, noData, error }

enum DetailStatusState { initial, loading, hasData, noData, error, succes }

enum SearchStatusState { initial, loading, hasData, noData, error }

enum AddStatusState { initial, loading, success, error }

enum FavoriteStatusState { initial, loading, hasData, noData, error }
